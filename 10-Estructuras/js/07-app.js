// 77. El Operador OR para que se cumpla al menos una condición

const efectivo = 300;
const credito = 400;
const disponible = efectivo + CredentialsContainer;
const totalPagar =600;

if( efectivo > totalPagar || credito > totalPagar || disponible > totalPagar){
    console.logo('si podemos pagar ')
}else{
    console.log('Fondos Insuficiente ')
}