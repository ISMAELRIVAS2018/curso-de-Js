const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}

// const nombre = producto.nombre
// console.log(nombre);

// destructuring
const{ nombre, precio, disponible} = producto;
cosole.log(nombre);
cosole.log(precio);
cosole.log(disponible);